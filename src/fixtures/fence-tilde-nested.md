How to document a fence inside a fence: the outer tilde fence carries a backtick fence as literal content.

~~~markdown title="example"
Use a code block like this:

```js
console.log("hi");
```

And close it.
~~~

The info string above (`markdown title="example"`) must survive on the opening line.
