Pasted from the terminal before lunch, never closed the fence.

```text
$ obsidian seek:search query="alpha sweep"
[seek] 12 results in 31ms

# this hash line is inside the open fence
## so is this one

Everything to end-of-file stays code.
